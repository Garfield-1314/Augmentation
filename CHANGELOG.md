# 更新日志 (CHANGELOG)

## [2026-05-13] - 全新可视化GUI与流程处理升级

### 新增 (Added)
- **PyQt5 可视化界面 (`qt/main_gui.py`)**：新增图形化客户端界面，支持脱离代码和终端执行。
- **拖放式流水线系统**：引入类似 Altium Designer (AD) 元器件拖拽的交互体验，支持动态组装功能模块流水线。
- **实时预览与非阻塞执行**：支持运行前刷新预览效果图；引入 `QThread` 多线程机制处理耗时操作，带有滚动进度条与任务状态反馈，防止界面假死。
- **中文路径支持**：支持 OpenCV 解析和写入包含非 ASCII 字符（如中文）的文件路径。

### 更改 (Changed)
- **UI 描述精简**：优化界面元素文字标签，使之保持干净简洁无多余方向说明。

### 移除/暂时下线 (Removed/Suspended)
- **暂时禁用 `yolo_Au.py`**：由于升级新版 `albumentationsx` (替代原版 `albumentations`) 后 `BboxParams` 在 YOLO 格式配置上存在的兼容性错误，暂时将该模块从主界面入口及自动流程中解绑，以待后续修复。

## [2026-05-08] - 局部光照增强与兼容性修复

### 新增 (Added)
- **局部光照模拟**：在 `Augmentation_CV.py` 中新增 `LocalIllumination` 算法。
  - 支持通过二维高斯掩码模拟真实的局部强光（过曝）与局部阴影（过暗）效果。
  - 新增 `batch_local_illumination` 批量处理接口，支持随机生成多个光斑/阴影点。

### 修复 (Fixed)
- **Albumentations API 兼容性**：修复了 `image_mask_AL.py` 中 `A.Affine` 变换因使用过时参数 `cval` 导致的警告。已统一切换为新版 API 的 `fill_value` 参数。

## [2.0.0] - 2026-05-07

### 重构 (Refactor)
- **全面模块化**：重构了 `Augmentation_AL.py`, `Augmentation_CV.py` 和 `background.py`。
- **解耦逻辑**：将底层图像处理算法与上层目录遍历逻辑分离，提高了代码的可复用性。
- **标准化接口**：所有模块现在均支持通过参数配置（如 `input_dir`, `output_dir`）进行调用，并提供统一的批量处理函数（如 `batch_*`）。

### 新增 (Added)
- **统一入口**：新增 `main.py` 作为项目示例入口，演示如何组合调用不同模块的功能。
- **通用处理引擎**：在 OpenCV 模块中引入了 `process_directory` 模板函数，可快速将任何单图处理逻辑扩展为批量处理。
- **增强健壮性**：全局引入 `tqdm` 进度条显示，并增加了完善的文件格式检查与错误处理。

## [2026-04-24] - 增强算法优化与修复

### 修复 (Fixed)
- **智能定位逻辑冲突修复**：解决了当小图旋转后尺寸（或要求 `min_visible=1.0`）超过背景 ROI 时，由于数学范围无效导致无法合成图像的报错问题。
- **Alpha 通道处理修复**：解决了直接对 RGBA 图像应用 Albumentations 颜色增强组件时产生通道数不匹配（Ranges Error）及 `HueSaturationValue` 异常的问题。采取了 RGB/Alpha 分层处理策略。

### 新增 (Added)
- **几何侧视效果**：集成了 `Albumentations.Affine` (Shear) 变换。通过对 X 轴进行错切（-25° 到 25°），模拟了从左侧/右侧观察小车的侧向透视感。
- **自动裁剪功能**：实现了小图中心点在 ROI 内随机分布，且允许小图边缘超出背景边缘，由 PIL 自动完成超出图像边界部分的裁剪。

### 优化 (Changed)
- **代码精简**：移除了冗余的 `roi_list`, `roi_dict` 及相关的全图验证逻辑。现在代码统一使用背景全图作为默认放置区域，简化了函数签名。
- **随机性增强**：将小图增强（颜色+几何）移入增强循环内部，确保即使是同一对背景和小图，生成的每一张增强图在角度、形状和色偏上都具有唯一性。
